# microservice specific routes

POST       /:safeId/:postcode/subscribe       controllers.SubscriptionController.subscribe(safeId: String, postcode: String)
GET       /:tavcReferenceNumber/get-subscription       controllers.SubscriptionController.getSubscription(tavcReferenceNumber: String)